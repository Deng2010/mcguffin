# ========== Rust (server) ==========
server/target/

# Runtime data files — contains sensitive/instance-specific data
server/mcguffin_data.json
server/mcguffin_data.json.bak
server/backups/
server/plugins/
server/src/*.rs.corrupted

# ========== React / Vite (web) ==========
web/node_modules/
web/dist/
web/.env
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.d.ts.map

# ========== Web 插件 ==========
# 插件一律以 .zip 包经管理后台上传安装（资产落在数据目录，不入版本库）；
# 主仓库只保留插件系统本身（registry.ts / types.ts / PluginPage.tsx / index.ts / sdk/）。

# ========== Package manager lock files ==========
# bun.lock 需要提交以确保 CI 可复现构建
# web/bun.lock
web/package-lock.json

# ========== General ==========
.DS_Store
*.swp
*.swo
*~
*.bak
.env
restart.sh
server/mcguffin_data*
.omo

# ========== AI-Generated Python Script ==========
server/*.py
web/*.py
backups/
mcguffin_data.db*
nohup.out

REFACTOR.md
.hermes/
.pi/
Containerfile
server/recipe.json
