# ========== Rust (server) ==========
server/target/

# Runtime data files — contains sensitive/instance-specific data
server/mcguffin_data.json
server/mcguffin_data.json.bak
server/backups/
server/plugins/
server/src/*.rs.corrupted

# ========== React / Vite (web) ==========
web/node_modules/
web/dist/
web/.env
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.d.ts.map

# ========== Web 插件（本地专属，不入版本库）==========
# 这两个插件是部署/本地专属的代码注册插件，不随主仓库分发：
#   - lollipop-rank（榜榜糖）：站点特定玩法
#   - team-members：团队成员页的插件化实现
# 目录保留在工作区以便本地开发与构建，但 git 不再跟踪。
# 构建期由 registry.ts 的 import.meta.glob("/src/plugins/*/index.ts") 发现，
# 目录不存在时静默跳过，不会导致构建失败。
# 注意：克隆仓库后这两个插件不会出现（属预期行为）。
# 插件发现机制、SDK 与运行时（registry.ts / types.ts / PluginPage.tsx / index.ts / sdk/）
# 仍在仓库中，不受以下规则影响。
web/src/plugins/lollipop-rank/
web/src/plugins/team-members/

# ========== Package manager lock files ==========
# bun.lock 需要提交以确保 CI 可复现构建
# web/bun.lock
web/package-lock.json

# ========== General ==========
.DS_Store
*.swp
*.swo
*~
*.bak
.env
restart.sh
server/mcguffin_data*
.omo

# ========== AI-Generated Python Script ==========
server/*.py
web/*.py
backups/
mcguffin_data.db*
nohup.out

REFACTOR.md
.hermes/
.pi/
Containerfile
server/recipe.json
